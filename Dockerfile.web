FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache git

# PNPM kur
RUN npm install -g pnpm

# Repo klonla
RUN git clone https://github.com/Nerimity/nerimity-web.git .

# Bağımlılıkları yükle
RUN pnpm install

# Build et
RUN pnpm build

EXPOSE 3000

CMD ["pnpm", "preview"]
